import { DaysPassedCountPipe } from './days-passed-count.pipe';

describe('DaysPassedCountPipe', () => {
  it('create an instance', () => {
    const pipe = new DaysPassedCountPipe();
    expect(pipe).toBeTruthy();
  });
});
